# 护理刷题自动答题工具 — 需求文档

> 本文档汇总目前为止所有已确认的需求、界面事实和决策，后续改动先更新本文档再动代码，避免边改边忘。
> 工程路径：`E:\code\new`，包名 `com.quizhelper.dumptool`。编译/装机命令见 [`reference-build-commands`](C:\Users\huangcan\.claude\projects\E--code-new\memory\reference-build-commands.md) 内存条目。

## 1. 背景与目标

给女友的"护理助手"App（`com.ecloudinfo.hulizhushou`，宜云健康/虚拟医院）刷题任务做自动答题。
目标App的题目正文用画布/WebView渲染，**无障碍服务读不到任何节点**（已逐窗口验证，App窗口是空的），因此**必须走「截图(takeScreenshot) + ML Kit中文OCR + dispatchGesture坐标点击」路线**，不能指望读节点拿文字/做点击。

测试机：小米 MIUI，Android 14 (API34)，分辨率 1080×2400。两台adb设备连接时必须显式 `-s 564e42ec` 指定物理机（另一台是未用的emulator-5554）。

## 2. 目标App的界面行为（已逐屏截图确认的事实）

- **任务详情页**：含"任务详情/完成条件/出题规则/错题集/开始答题"等特征词；顶部有题库**标题**（如『"三基"分册6外科护理学(烧伤+移植+神外)』）+ 一行小字**项目**（如『急诊科2026年考试』）。点"开始答题"进入做题。
- **答题页(QUESTION)**：顶部题号"N." + 类型"单选题"或"多选题"(红字，OCR常认错成"半选题"等) + 题干；选项A~E每个是「圆圈字母+文字」；底部"确定"按钮(灰=未选,粉=可点)；最底导航条 上一题|纠错上报|下一题。（题号N每轮重新计数的细节见下一条"轮/组边界"说明）
- 选中某项→圆圈变绿、确定变粉。点确定→进入反馈页。
- **反馈页(FEEDBACK)**：选错的圈变红、正确项圈变绿，并显示文字 `正确答案:X你的答案:Y`（直接是文字，OCR读取即可，不需要识别颜色）；按钮变 AI讲题|下一题。
- 一组题目第1轮固定 **15题**；**做错的题会按"轮"重复出现**（第2轮只含第1轮的错题，第3轮只含第2轮剩下的错题……），直到某一轮全部答对为止。**每一轮的题号都重新从1开始排列**，不是接着上一轮的题号继续往后数。每一轮的最后一题，反馈页按钮会从"下一题"变成"提交"。点"提交"后：这一轮**还有错题**时，**直接进入下一轮的题，不会弹出任何确认弹窗**；这一轮**全部答对**时，点"提交"会弹出"确定要提交么"弹窗（**SUBMIT_DIALOG**），点弹窗里的确定表示**整组真正完成**。即 SUBMIT_DIALOG 在一个题库里**只出现一次**（最后全对那一轮），不是每轮提交都会出现。
- 一组全对/提交完算"完成一组"，每组积分 = 题数。**错题没有惩罚**，可以放心试错来学答案。
- **题型：单选题为主，多选题确实存在，只是目前还没实测遇到过**。`ScreenModel.isMulti` 字段和 `tapSequence` 的多点位连续点击支持要保留，遇到真实多选题时要重点盯一下这条逻辑是否工作正常。

## 3. 自动答题核心规则

整体是三层，从底层往上：**盲选（核心）→ 暴力模式（盲选 + 顺便OCR建题库）→ 智能模式 / 自动化（在暴力预选之上用题库答案替换 + 整组循环刷分）**。实现顺序也按这个来：先盲选，再暴力，最后自动化。

三者的关系：每次作答都先跑一遍暴力盲选逻辑算出一个"预选答案"，再看"智能模式flag"是否开启——开了就把预选答案替换成题库里查到的该题答案（查不到/不可信则保留盲选预选）。所以**智能模式 = 暴力流程 + 一层"查表替换"，盲选是两种模式共同的地基**。

### 3.1 盲选——核心底层（两种模式都依赖）

- **轮换节奏**：没学到答案时，盲选当前"轮"锁定的字母——第1轮所有未知题统一选 **A**，进入下一轮统一改 **B**，再下一轮 **C**……（对选项数取模循环）。轮/组边界的界面事实见第2节（最后一题按钮变"提交"后，这一轮没全对就直接进下一轮、不弹窗；全对才弹出 SUBMIT_DIALOG，确认 = 组真正完成）。
   - 举例：第1次进入任务，第1轮=15题（题号1~15）；第15题（本轮最后一题）按钮变"提交"→点提交→假设这15题里有5道错的→**没有弹窗**，直接进入第2轮，题号重新从1数到5；第2轮第5题按钮变"提交"→点提交→假设又错2道→**没有弹窗**，直接进入第3轮，题号从1数到2；……直到某一轮（比如第4轮）全部答对，那一轮点"提交"才第一次弹出 SUBMIT_DIALOG，确认后才是整组真正完成。
   - **盲选字母推进的唯一触发点 = 上面的"轮"边界**（每轮最后一题按钮变"提交"且点了提交），不是按题、也不是按其它任何事件推进。
   - **⚠️ 已落地代码触发点接错了（待修复见6.5）**：`blindRound++` 现在写在 `SUBMIT_DIALOG`（确认弹窗）分支，而弹窗只在"整组最后全对那一轮"出现一次——所以组内各轮之间 `blindRound` 不推进、字母一直不换，未知题答案若不等于当前字母就一轮轮一直错、卡死。**正确的触发点是"反馈页动作按钮=提交"这个每轮都会出现的事件**（点提交→进下一轮时 `blindRound++`）；题号从大变小只能做辅助判据（题号可能一直是1）。**这很可能是"卡死循环"的真正根因之一**，比"未识别弹窗"更基础。
- **有限轮内必中（仅单选题）**：每轮统一换一个字母、选项最多5个，最多5轮盲选必然命中一次。这是**暴力模式**的收敛保证；**智能模式**因为第一次错后就从反馈页学到答案、下一轮直接命中，通常1轮就够。⚠️ 此保证**不适用多选题**（单字母盲选凑不出多字母答案，见5.5）。
- **不能永久卡死在同一题上**：即便OCR识别、坐标计算出了任何没预料到的差错，也必须有兜底能跳出死循环，而不是无限重复同一题。已实测出现过两类卡死：① 第一轮提交后进入第二轮错题、题号重排时反复卡在同一题；② 题库识别错误时一直卡住。
   - **无法自愈时的处理**：如果连各种兜底/自救尝试都无法让程序自己恢复正常（比如长时间停在 UNKNOWN，怎么点都恢复不了），不应继续死等或无限重试，而是要：① 提示用户"卡住了，需要人工介入"；② 按 3.3 的机制回收本次运行的临时资源；③ 停止自动循环，停在"等待用户手动点开始"的状态。具体"判定为无法自愈"的阈值、提示方式待讨论，见 6.6。

### 3.2 暴力模式——盲选 + 顺便OCR建题库

- 作答完全靠 3.1 的盲选，**不读题库**来决定点哪个选项。
- 但**照常做OCR**（识别题干 + 反馈页的"正确答案:X"），把学到的"题→答案"**写进题库**。因此暴力模式跑几遍就能把一个题库的答案攒出来，可以先用暴力走几遍、再切智能模式秒答。
- **答案记忆按题库分开存**：题库标识 =「任务详情页」的 标题 + 项目，两者都相同才算同一题库；切换题库时先存旧库再载入新库；每个题库存一个独立文件（含标题/项目/更新时间/题→答案），方便以后单独删除过期题库（已在 `MainActivity` 题库管理界面里做了"删除"按钮）。
   - **新发现的bug（待修复，方案见6.4）**：这个"标题+项目完全相等"的判断过严——实测同一个真实题库被拆成了两个题库文件，学到的答案没法共享。需要改成更鲁棒的匹配方式（关键片段提取或模糊匹配），具体算法待讨论确认，不要在没确认方案前就动手改。

### 3.3 智能模式 / 自动化——在暴力之上叠加

- **智能模式 = 暴力预选 + 查表替换**：先按 3.2/3.1 算出盲选预选答案，再检查"智能模式flag"是否开启；开了就用题库查到的该题答案**替换**预选；查不到、或该存档答案连续判错被标记不可信（`failCount`/`distrusted`）则退回盲选预选。
- **选错时的学习（覆盖）规则**（两种模式都生效，因为都写库）：反馈页的"正确答案"是绝对权威。当我们选了某个答案、反馈页却显示错了时，用 `yourIdx`（反馈页"你的答案"，即实际选中项）和我们这一题"打算点的下标"对比来决定怎么处理：
   - `yourIdx == 打算点的下标` 且 `yourIdx != correctIdx` → 说明**答案本身错了**（不是手滑），用 correctIdx **覆盖**题库里这条，下次这题再出现就秒对。
   - `yourIdx != 打算点的下标` → 说明**点击没点中**（点偏/被遮挡），不是答案错 → **不覆盖题库**，避免把一条本来正确的答案改坏；这次按"点击失败"处理。
   - 同一题连续判错到阈值（`failCount`/`distrusted`，见第4节）→ 多半是"两道不同题被去重归到了同一个 key"之类的结构问题，覆盖也救不了（会来回横跳）→ 停止信任这个 key、退回纯盲选，保证不卡死。
   - 这套"区分答案错/没点中"的判断，正是 6.3"点下一题前再识图校验"要支撑的目的。
- **整组循环**：一组做完（提交+确认）后自动回到任务详情页，自动点"开始答题"进入下一组；做到设定的目标组数后自动停止。目标组数是**用户可改的设置项**，默认 `targetGroups = 14`，由用户按需修改即可，不需要去对任务页显示的分数（那个分数口径不用管）。
- **临时文件/资源要尽量自动回收，不能越用越大**。下面把"产生哪些、存哪、保留还是删、何时删"列清楚（均为代码实测确认）：

   **所有文件都在 App 的外部私有目录下**：`getExternalFilesDir(null)`，即 `/Android/data/com.quizhelper.dumptool/files/`，App 卸载时系统会整体清掉。其下只有两个子目录：

   | 目录/文件 | 由谁产生 | 内容 | 处置 |
   |---|---|---|---|
   | `banks/bank_<hash>_<len>.json` | `AnswerStore.persist()`，每次 `put()` 学到答案就落盘 | 每个题库一个JSON：`{title, project, updated, answers:{题干→正确字母}}` | **永久保留**（学习成果）。只能由用户在 `MainActivity` 题库管理界面**手动**逐个删除，自动回收**绝不**碰它 |
   | `dumps/step_*.txt` `dumps/ocr_*.txt` `dumps/node_*.txt` | `saveToFile()`，**仅**调试广播 STEP/OCR/NODES 触发 | OCR/节点/逐帧的纯文本导出（调试取证用） | **临时**，自动回收对象。注意：**自动答题本身不写 `dumps/`**，只有人手动发调试广播才会写 |

   - **不会产生截图文件**：目前没有任何截图(.png)被写到磁盘。截图只是内存里的 `Bitmap`，OCR 完即弃（见下条）；`dumps/` 里只是几个 KB 级的文本。
   - **内存资源（待办）**：每次 `captureScreen` 拿到的 `Bitmap`（1080×2400 ARGB_8888 ≈ 10MB）用完后**没有显式 `recycle()`**，靠 GC 回收。高频截图循环下这是真正更值得关心的"资源"。建议在 OCR 回调结束后显式 `bmp.recycle()`，列为待办（不属于"删文件"，但属于"回收资源"）。
   - **`clearTemp()` 的语义**：只删 `dumps/` 目录下的文件，**不动** `banks/`。`DumpAccessibilityService.clearDumps()` 直接委托给它。
   - **自动触发删除的时机**（已落地，见第4节）：
      - 每轮提交确认(`SUBMIT_DIALOG`)
      - `stopAuto()`（手动停止 / 达到目标组数自动停止）
      - 服务 `onDestroy`/`onUnbind`（系统关闭或解绑无障碍服务，走 `cleanup()`）
      - 进程崩溃前的未捕获异常兜底(`installCrashCleanup`，尽力而为——Android 不保证崩溃时还能跑代码)
      - 以上都不依赖 `MainActivity` 那个手动"清理临时文件"按钮（它只是额外的兜底入口）
   - **dev机器上的** `logcat_latest.txt` 是调试时手动重定向出来的，不在 App 关心范围内，已加进 `.gitignore`。
   - 后续若新增任何临时文件（比如第5节第2点设想的"把 UNKNOWN 画面存一份方便适配"），必须写进 `dumps/`（或同样纳入 `clearTemp` 范围），保持"临时文件只此一处、统一回收"。

### 3.4 运行流程——一个 step 的完整生命周期

自动答题是一个**定时循环**：每隔 `stepIntervalMs`（当前 1300ms）跑一个 step，一个 step = "截一次图 → OCR → 判断当前是什么页面 → 按页面类型做一次动作 → 排下一个 step"。代码入口 `scheduleStep()` → `loopStep()`，循环只在 `auto==true` 时继续。**每个 step 恰好截 1 次图、做 1 次 OCR**（约每 1.3s 一次，不是连续不停地截）。

一个 step 内部按顺序：

1. **隐藏悬浮条**：`setOverlayVisible(false)`。必须先把我们自己的悬浮控制条藏掉，否则它会被截进图里、甚至我们的点击会落到悬浮条上。等 120ms 让它真正消失。
2. **截图**：`takeScreenshot(DEFAULT_DISPLAY)` 拿到 `HardwareBuffer` → 转成 `Bitmap`(ARGB_8888) → `buffer.close()`。截图失败则本 step 直接结束、排下一个。
3. **OCR**：`OcrEngine.recognize(bmp)`（ML Kit 离线中文）出文字+每行包围盒。
4. **纠错 + 解析**：每行先过 `OcrFix.fix()` 纠错表（见6.1），再 `ScreenParser.parse()` 解析成 `ScreenModel`——判定页面类型 `kind`（QUESTION/FEEDBACK/TASK_DETAIL/SUBMIT_DIALOG/UNKNOWN）并算出要点的坐标、正确答案/你的答案下标等。
5. **按页面类型动作** `act(model)`：
   - **TASK_DETAIL**（任务详情页）：若 `groupInProgress` 为真（说明刚从做题状态回到这页）→ 判定**一组完成**：`groupsDone++`、`blindRound=0`、清空 `failCount`，达到目标组数就 `stopAuto()`。然后 `setBank(标题,项目)` 切到对应题库，`groupInProgress=true`，点"开始答题"。
   - **QUESTION**（答题页）：保存题干到 `lastQuestionText`；按 3.1/3.3 算出要点的下标（智能命中→存档答案；否则/不可信→`blindRound` 盲选字母）；`tapSequence` 依次点选项（多选用）再点"确定"。
   - **FEEDBACK**（反馈页）：把识别到的"正确答案"`store.put()` 写进题库（用 `lastQuestionText` 当 key，因为反馈页题干末尾会多出解析文字）；用 `yourIdx` 核实命中/失败、维护 `failCount`（见3.3、第4节）；点"下一题"。
   - **SUBMIT_DIALOG**（提交确认弹窗，只在整组全对那一轮出现）：点"确定"完成整组，`clearDumps()`。（⚠️ 现在 `blindRound++` 也写在这里，是接错了对象——应挪到"反馈页提交按钮"事件，见3.1/6.5。）
   - **UNKNOWN**：目前只打日志"等待"、什么都不做——这正是死等卡死的来源（见5.2，待加自救兜底+无法自愈时停止）。
6. **收尾**：`finishStep()` → `setOverlayVisible(true)` 恢复悬浮条 → `scheduleStep()` 排下一个 step。

> 注：`STEP`/`OCR`/`NODES` 是另一套**手动调试广播**，只截图+解析+把结果写进 `dumps/` 文本，不点击、不参与上面的自动循环。

## 4. 已落地实现清单（对应第3节规则的落地状态，均已编译验证）

> 本节是"现状快照"：第3节是规则/设计，本节逐条说明落地状态、并标注对应的 3.x / 6.x。图例：✅已落地并编译验证、⚠️已落地但有已知问题待改、🔲未实现。

- ✅ **截图→OCR→解析→动作的主循环**（对应 3.4）：`scheduleStep`/`loopStep`/`captureAndParse`/`act` 已跑通五种页面分派。
- ✅ **选项坐标行合并阈值修复**（对应 3.4 第4步解析；`ScreenParser.clusterRows` 50px→65px）：根因是选项文字换行后多出的单字残行（如"血"字单独成行）离主选项行约51px，超过原50px阈值被误判成独立"选项行"，把后面所有选项坐标顶歪一位，导致"答案查对了但点的位置是错的"。已用真实卡死案例（高灌注综合征题）截图日志定位并验证修复（修复后该题第一次点击就直接命中真实坐标）。
- ✅ **"你的答案"核实机制**（对应 3.3 选错学习 / 3.4 第5步 FEEDBACK；`ScreenParser.yourIdx`）：解析反馈页 `正确答案:X你的答案:Y` 里的Y部分。之前只看「打算点哪个下标」判对错，改成看反馈页真实显示的"你的答案"，才能查出"以为点对了、实际点击没命中"这类问题。
- ✅ **已存答案连续失败计数 + 自动降级为盲选**（对应 3.3 / 3.1 跳出死循环；`failCount`/`distrusted`/`failLimit=2`）：某题用存档答案点击后，反馈页"你的答案"连续2次≠"正确答案"，就认为存档不可信，改走 `blindRound` 盲选。`failCount` 在开始新一轮、或每组完成时清空。
- ✅ **临时文件自动回收**（对应 3.3）：`clearDumps()` 复用 `AnswerStore.clearTemp()`，在四处触发——SUBMIT_DIALOG 提交确认、`stopAuto()`、服务 `onDestroy`/`onUnbind`、未捕获异常兜底 `installCrashCleanup`。只清 `dumps/`，不动 `banks/`。
- ⚠️ **盲选轮换节奏**（3.1）：已实现并跑通，但 `blindRound++` 接在 SUBMIT_DIALOG 弹窗（组末才出现）、几乎不在轮间触发，应改接"反馈页提交按钮"，待改（见 5.1 / 6.5）。
- 🔲 **暴力/智能模式开关 + 目标组数设置**（3.2/3.3、6.2）：未实现，`targetGroups` 仍硬编码；模式开关、写库去重、选错覆盖学习均未落地。
- 🔲 **题库同一性改模糊匹配**（3.2、6.4）：未实现，仍用"标题+项目完全相等"。
- 🔲 **UNKNOWN 自救 + 无法自愈时停止**（3.1、5.2、6.6）：未实现，`UNKNOWN` 分支目前只"等待"。
- 🔲 **提交前取色复核**（6.3 方案B）：未实现，需新增按坐标取像素颜色能力。
- 🔲 **OCR纠错自动学习**（6.1）：未实现，`OcrFix` 仍是纯手写表。
- 🔲 **step 间隔可调**（6.7）：未实现，`stepIntervalMs` 仍硬编码 1300ms。
- 🔲 **题库展开查看 + 手动修正**（6.8）：未实现，管理界面目前只能整库删除，不能看/改条目。
- 🔲 **截图 Bitmap 显式 recycle**（3.3、5.7）：未实现。

## 5. 当前已知缺口（待处理，优先级从高到低）

1. **（最高优先级）blindRound 推进的触发点接错了对象**：见第3.1节/6.5——`blindRound++` 现在接在 `SUBMIT_DIALOG`（确认弹窗）分支，而弹窗只在整组最后全对那一轮出现一次，组内各轮之间根本不触发。结果 `blindRound` 在一个题库做完前几乎不变、盲选字母不换，未知题答案若不等于当前字母就一直错、卡死——**很可能是"卡死循环"的真正根因**。修复方向：改接"反馈页动作按钮=提交"这个每轮都有的事件（题号从大变小只能做辅助，可能一直是1）。
2. **未识别的弹窗/插页导致死等**：`ScreenParser` 只认 QUESTION/FEEDBACK/TASK_DETAIL/SUBMIT_DIALOG 四种正常页面，碰到其它弹窗会落到 `UNKNOWN`，而 `act()` 的 `UNKNOWN` 分支只打日志"等待"，不会做任何尝试，本次会话实测在这个状态卡了近7分钟。
   - 那次卡死时弹出的就是已经能识别的"确定要提交"确认框(SUBMIT_DIALOG)本身，**不是**一个全新、未识别的弹窗。所以根因要从别处找，目前怀疑方向：① 点 `dialogConfirm` 坐标没有真正点中/生效，弹窗一直没消失，但某些帧OCR碰巧没能稳定匹配到"确定要提交"/"所有题目已作答"关键词（噪声/遮挡），于是这一帧被分类成 `UNKNOWN`，下一帧又识别回 SUBMIT_DIALOG，反复横跳但弹窗从未真正被关掉；② 还有别的、目前没想到的原因；③ 现在看也可能和上面第1点有关——如果当时其实是中间轮次（不该有弹窗），那个"弹窗"可能本来就是别的东西，需要结合第1点的修复重新观察。
   - 取证还没拿到：本次会话曾用 `STEP` 想抓这个弹窗的OCR原始内容，但抓到的时候弹窗已经消失/翻页过去了（抓到的是正常的FEEDBACK页）。**下次复现，第一时间发 `STEP` 广播**（不要等，不要先做别的），连续抓两三次（间隔1~2秒）把这期间每一帧的OCR原始行列表都存下来，才能看出弹窗到底有没有被点掉、点击坐标和弹窗实际按钮位置是否吻合。
   - 通用兜底（即便取证前也可以先做）：连续N次 `UNKNOWN` 后尝试点截图里能找到的"确定/关闭/X/我知道了"类按钮，或者退一步用"返回"键，作为"不认识但先尝试自救"的兜底，避免无限期等待。若连这些自救都不能让画面恢复，再走"提示用户+回收资源+停止"（见第3.1节"无法自愈时的处理"、第6.6节），而不是无限重试。
3. **（新发现）题库匹配过严，同一题库被误判成两个**：见第3.2节"答案记忆按题库分开存"——实测同一个真实题库被拆成两个题库文件。修复方向（待讨论，见6.4）：用标题/项目里的稳定关键片段或模糊匹配代替"完全相等"判断。
4. **任务详情页标题行可能丢字**：`ScreenParser` 用 `cy in 200..410` 配合"含数字/单选/多选/选题/判断"做表头行识别，已发现至少一例"单选题"和题干第一行被OCR粘在同一行从而整行被当表头排除、丢失题干开头几个字（如"病人,男,65岁。高血压及糖尿病"）的情况；目前靠 fuzzy-match 容忍，但严格来说题干文本不完整，长期可能影响存取key的稳定性，未正式修复。
5. **多选题盲选无解 → 暴力模式会卡死**：盲选每轮只点 1 个字母（`blindRound % size`），而多选题正确答案是多个字母，单字母盲选**永远不可能命中**。智能模式还能靠"第一次错→反馈页学到完整答案(如ABD)→下一轮命中"在1轮后解决；但**暴力模式从不读题库**，对多选题就会一轮轮一直错、永远做不完那一组、卡死。需要给盲选补一套多选策略（如按反馈页学到的答案数 k 盲选组合，或多选题在暴力模式下也破例查一次表），方案待定。多选题目前还没实测遇到，但属于"一遇到就卡死"的隐患。
6. **3.3"选错只在确实答错时才覆盖"尚未落地，且缺前置数据**：当前代码在 FEEDBACK 分支是**无条件** `store.put(correctIdx)`，且 `failCount` 只判 `yourIdx != correctIdx`（没区分"答案错"还是"没点中"）。要实现 3.3 的精细规则，QUESTION 分支需把"这次打算点的下标"一并存下来，FEEDBACK 才能比对"实际选中 vs 预设"。这是 3.3 落地的前置改动。
7. **（低优先）截图 Bitmap 未显式 recycle**：`captureScreen` 每次拿到的 `Bitmap`（≈10MB）用完没 `recycle()`，靠 GC 回收；自动答题是高频截图循环，建议在 OCR 回调结束后显式回收（见第3.3节）。不是磁盘文件问题，但属于"资源回收"。

## 6. 已决定要做、待实现的方案

> 不再单设"待澄清问题"章节：有疑问当场沟通确认；未实现的方案统一记在本节，每条把"已定的"和"未定的"分开写清。

### 6.1 OCR纠错替换表——增加"自动学习"，置信度达标才能入表
现状：`app/src/main/java/com/quizhelper/dumptool/OcrFix.kt` 是一个 `(错误文本, 正确文本)` 的硬编码列表，`toLines()` 对每一行OCR结果都会过一遍（如 `"苔案"→"答案"`、`"ー"→"一"`），目前只能手动加行。

**目标**：做成自动学习，但要求足够高的把握（原始口径"95%以上"）才自动入表。

**置信度算法**：复用 `AnswerStore` 已有的模糊匹配基础设施（Levenshtein 相似度，阈值0.82）。当两段文本被判定为"同一道题"（相似度达标但不是完全相同）时，回溯它们之间具体差在哪几个字符，就是一条"候选替换对"（比如这次差在"苔"/"答"）。把这种候选对在**整个使用过程**（不分题库）里出现的次数和方向一致的比例都记下来（持久化成一个新的小文件，类似 `AnswerStore` 的JSON存法）：
- 样本量达到下限（比如 ≥5 次观察）**且**方向一致比例 ≥95%（即几乎每次出现这种差异，都是同一个方向的替换，没有出现过反方向的情况）时，才自动把这条 `(错误文本→正确文本)` 写入持久化的"学得替换表"，之后自动套用到所有后续OCR结果，和手工表一起生效。
- 没达标的候选对只是先记着次数，不生效，不影响现有逻辑。
- 学习信号在暴力和智能两种模式下都会产生（两种模式都做OCR、都建题库，见3.2/3.3）。**已确认要做写库去重**：写入前先拿当前题对题库里的现有key做一次模糊匹配，命中就更新同一条、不新建。这次模糊匹配比较正好就是OCR纠错"候选替换对"的来源，所以**暴力模式也会产生纠错学习信号**。
- **去重、匹配、自学共用同一套"求吻合度"**：题干去重 = `canonical()` 比中同一题用同一 key；OCR 自学 = 同一次比对里抽出文字差异。是同一次比较的两个产出，不是两套算法。
- **把握的来源（关键）**：纯相似度阈值不牢——高了漏并、低了误并（案例组合题、OCR噪声都会出例外）。"每个题干都不一样"是有利条件（真不同的题距离大）。两条增稳措施：① **阈值取保守、偏向漏并**（误并危害大且难被发现，漏并只是多几条、可在6.8合并）；② **把"正确答案是否一致"作为文字相似度之外的独立佐证**。最终兜底是 **6.8 题库可查看修正**（人工纠错）。具体阈值后续专门定。

**未定**：自动入表的两个门槛——样本量下限（暂定 ≥5 次）、方向一致比例（暂定 ≥95%）——需要调整，具体数值未定，写这块代码前先敲定。

### 6.2 暴力 / 智能两种模式——同一套底层 + 一个UI开关切换
核心架构见第3节（3.1盲选 / 3.2暴力 / 3.3智能）。这里只记实现要点。**关键修正**：旧版本写的"暴力模式完全不读不写题库"已作废——暴力模式照常OCR、照常写题库，只是作答不查表。两种模式只差"作答时是否用题库答案替换盲选预选"这一层。

- **暴力模式（flag关）**：每道未知题按 3.1 盲选作答（不查题库决定点哪个）；但仍OCR、把反馈页学到的答案写进题库。胜在简单、不被题干模糊匹配出错拖累；代价是同一题库下次重复出现也不会"秒对"，要多跑几轮。适合先用它把一个新题库的答案攒出来。
- **智能模式（flag开）**：在暴力盲选预选的基础上，用题库查到的该题答案替换预选；查不到/连续判错不可信(`failCount`/`distrusted`)则保留盲选。
- 两种模式共享同一套"轮/组边界"和 `blindRound` 兜底机制（3.1），区别只在 3.3 那层"查表替换"。

**默认模式（已定）：默认暴力模式**，可在设置页勾选切换到智能。理由与推荐用法：新题库还空时，智能模式查不到答案、跟暴力没区别还多一层查表，所以**推荐先用暴力跑几遍把答案攒进题库，攒得差不多了再切智能秒答**。两种模式区别见上面两条（暴力=纯盲选作答+照样建库；智能=暴力预选之上用题库答案替换）。

**开关位置（已确认）**：**不放悬浮控制条**，放在 `MainActivity` 的设置页面里（和题库管理列表同一个Activity）。因为开关在 `MainActivity`、实际生效在 `DumpAccessibilityService`，两者是不同组件，需要一个持久化的中间层（如 `SharedPreferences`）传递状态，而不能像悬浮条按钮那样直接改服务内存里的字段。

**待实现**：
- `MainActivity` 设置页加一个模式开关（如 `Switch`/`CheckBox`），状态写入 `SharedPreferences`（建议复用 `AnswerStore` 所在的偏好文件或新开一个专用文件）。同一个设置页再加几个输入框：**目标组数**（默认14，替代硬编码 `targetGroups`）、**step 间隔**（见6.7，替代硬编码 `stepIntervalMs=1300ms`）、**UNKNOWN 连续次数阈值**（默认5，见6.6），都用户可改、存同一份 `SharedPreferences`。
- `DumpAccessibilityService` 每次需要判断时从同一个 `SharedPreferences` 读取当前值（或在 `onSharedPreferenceChangeListener` 里缓存，避免每帧都读文件）——这样运行期间在 `MainActivity` 切换，正在跑的服务下一次截图循环就能读到新值并立即生效。
- `act()` 的 `QUESTION` 分支：先算盲选预选；若 flag=智能，再用 `store.get()/keyFor()` 查表替换（含 `failCount`/`distrusted` 不可信回退）。`FEEDBACK` 分支：两种模式都跑 `store.put()` 把学到的答案写进题库（这是和旧设计最大的不同），同时跑 yourIdx/correctIdx 校验日志。
- 开关读写的 `SharedPreferences` 默认值设为"暴力"。

### 6.3 点"下一题/提交"之前再识图校验一次——方案B（提交前主动复核选中态）
**这个功能的目的**：确认"实际选中的答案 == 我们预设/打算点的答案"，从而让 3.3 的"选错时覆盖学习"能安全地分清到底是"答案本身错了"（该覆盖题库）还是"点击没点中"（不该覆盖）。换句话说，它是 3.3 学习规则的前置保障——只有先确信"选中==预设"，那条"yourIdx==预设 且 yourIdx!=correct → 覆盖"的判断才不会误伤一条正确答案。

这里有两个不同时机要分清，**两者不冲突，可以只做一个或都做**：

- **事后判断（几乎免费，已具备）**：反馈页本来就显示"你的答案"，`ScreenParser` 已解析成 `yourIdx`。所以"这次到底点中没点中"在反馈页**不用额外再OCR**就能拿到，3.3 的覆盖判断已经能用它。代价：只能"事后"发现点偏了（这一题已经提交、错了，没法补救），只能用来保护题库不被改坏。
- **事前主动复核（方案B，待实现）**：在 QUESTION 页点了选项、点"确定"**之前**，对选中圈取像素颜色，确认我们打算点的那个圈真的变绿了；没变绿（点偏/没生效）就**当场重点一次**再提交。代价：要新增取色能力；好处：能在提交前就把点偏纠正掉，连"错一题"都省了。

- **方案A 不采用**（仅记录：纯OCR复核反馈页文字、防反馈页OCR噪声）。

**方案B 的实现前置条件（尚未开始）**：需要**读屏幕颜色**（选中圈变绿、确定按钮变粉），而当前 ML Kit OCR 流程只读文字、不读颜色，**是一个全新能力，现有代码里没有**。做法很可能是对 `takeScreenshot()` 返回的 `Bitmap`/`HardwareBuffer` 在指定 `XY` 坐标取色，判断是否接近"绿色已选中"，再接入 QUESTION 分支：点完选项坐标→取色校验是否变绿→变了才点"确定"，没变就重试点击。具体取色阈值/重试次数待设计。

**未定**："点下一题前再校验"——是只用反馈页现成的 `yourIdx` 做事后判断（免费、够保护题库），还是仍要按方案B在答题页点确定前主动取色复核（多一层防点偏+当场重试），还是两个都做。

### 6.4 题库匹配从"标题+项目完全相等"改成关键词/模糊匹配（新发现的bug，方案待讨论）
现状bug：实测中同一个真实题库被现有逻辑判断成了两个不同的题库文件（学到的答案没法共享，相当于白学一半）。根因很可能是"标题+项目完全相等才算同一题库"的判断太严格，两次OCR出来的标题/项目字符串只要有一点噪声/顺序差异就会被当成"不同题库"。

可用的稳定关键片段：标题括号里的内容（如"烧伤+移植+神外"）、项目里的年份+科室组合（如"急诊科2026"）——这些比整段标题文字更稳定。

**候选方案**：
- 不要求标题+项目整段文本完全相等，改成提取其中的"稳定关键片段"做匹配（比如项目里的纯数字年份+科室关键词、标题括号里的内容）；
- 或者更简单：对标题和项目分别做模糊相似度比较（复用 `AnswerStore` 已有的Levenshtein相似度基础设施），相似度超过某阈值（比如≥0.85）就认为是同一题库，不要求完全相等；
- 两种思路也可以结合：先比关键片段，再用整体模糊相似度做兜底。

这跟 6.1 是同一类"求吻合度"问题，同样复用 `AnswerStore` 的相似度基础设施，同样面临"把握不牢"。区别是这里没有"正确答案"那种独立佐证，只能靠标题/项目本身——所以更要取**保守**阈值（偏向判成同库，避免把同一题库拆开导致白学一半）。最终也以人工兜底：6.8 题库管理界面可让用户看到被拆成两个的同名题库、手动合并（合并功能列入 6.8 的可选项）。

**未定**：用"关键片段"、"整体模糊相似度"、还是两者结合，以及具体阈值。

### 6.5 blindRound 推进信号——接到"提交按钮"，不是接弹窗
先把事实理清（这是两种不同的"提交"）：

1. **每一轮的边界 = 反馈页动作按钮显示"提交"**（每轮最后一题，按钮从"下一题"变成"提交"）。点这个"提交"按钮：
   - 这一轮还有错题 → **直接进入下一轮**（不弹窗）。
   - 这一轮全部答对 → **才弹出"确定要提交"确认框（SUBMIT_DIALOG）**，确认后整组完成。
2. 所以"整组完成 = 弹窗"，把**组完成检测**绑在 SUBMIT_DIALOG 弹窗上是对的。
3. **盲选字母推进（blindRound++）该接在"反馈页动作按钮=提交"这个事件上**（每轮都会出现），而不是接在弹窗上。

**当前代码的真实毛病**：`blindRound++` 现在写在 `SUBMIT_DIALOG` 分支里，而弹窗只在"整组最后全对那一轮"才出现一次——所以组内各轮之间 `blindRound` 根本不推进，盲选字母一直不换，未知题答案若不等于当前字母就会一轮轮一直错、卡死。**触发点接错了对象**（接到了只在组末出现的弹窗，应接到每轮都有的"提交"按钮）。这仍是"卡死循环"的重点怀疑根因。

**修复方向**：
- `ScreenParser` 在反馈页要能**区分动作按钮是"提交"还是"下一题"**（现在 `nextBtn` 不分二者），多一个标志位（如 `isSubmit`）。
- `FEEDBACK` 分支点的若是"提交"按钮（=本轮最后一题，即将进下一轮）→ `blindRound++`。
- `SUBMIT_DIALOG` 分支保留点确定（组完成检测），那里的 `blindRound++` 可去掉或保留无所谓（组完成时 `blindRound` 会清零）。
- **题号从大变小**可作为**辅助**判据，但**不能单独用**：题号可能"一直是1"（某轮只剩最后一题时，题号就是1，看不出变化）。所以以"提交按钮"为主信号，题号变化顶多做佐证。

**结论（已定）**：把 `blindRound++` 从弹窗(SUBMIT_DIALOG)挪到"反馈页那个'提交'按钮被点时"。剩下的只是实现细节（ScreenParser 加 `isSubmit` 标志位区分提交/下一题按钮）。

### 6.6 UNKNOWN无法自愈时——提示用户+回收资源+停止（已定）
行为：检测到"自己解决不了" → 提示用户 → 按第3.3节清理临时文件 → 停止自动循环 → 等待用户手动重新点击开始，而不是无限等待或无限重试。

**已确认的判定与处理**：
- 两段是**串联**关系，不是二选一：每帧 UNKNOWN 先按 5.2 的"通用自救"尝试（点"确定/关闭/X/我知道了"或返回键）；**连续 5 次 UNKNOWN 仍没恢复**（即自救也没用）才判定无法自愈、走停止。
- **阈值默认 5 次，做成用户可设置**（放 6.2 那个 `MainActivity` 设置页，存同一份 `SharedPreferences`，默认5）。
- 触发后**弹窗提示 + 停止 + 等待用户操作**。弹窗优先用能在 App 不在前台也看得到的方式（系统通知栏 Notification 比 `toast()` 可靠；具体用哪种实现时定）。

**实现备注**：题库学到的答案是即时落盘的，停止时不用额外处理；停止后由用户手动重新点"开始"继续。

### 6.7 step 间隔做成用户可调，并实测最佳值
现状：一个 step 的间隔 `stepIntervalMs` 硬编码 **1300ms**（见3.4）。1.3s **偏慢**，但太快也会出问题——因为每个 step 之前要先隐藏悬浮条、等界面/动画稳定再截图，太短会截到上一页的残影、动画中间态、或点击还没生效就截下一张，导致解析错乱。

**要做**：把 step 间隔变成**用户可调的设置项**（和6.2的模式开关、目标组数放同一个 `MainActivity` 设置页，存同一份 `SharedPreferences`，运行期可改、下一个 step 生效）。给一个合理范围和默认值，让用户按自己手机的流畅度自己压到最快又不出错。

**待确认/待实测**：
- 安全下限大概是多少？太快出问题的根源主要是"点击→界面响应→动画"这段延迟，不同机型不一样，需要在测试机上实测：从某个值往下压，看从哪个值开始出现"截到残影/解析错乱"。
- 可调范围和默认值定多少（比如范围 500~2000ms、默认先给个比 1300 小一点的值）？由实测结果决定。
- 是否需要把"截图前的固定等待"（现在是隐藏悬浮条后等 120ms）也一并暴露成可调，还是只调主间隔就够？

### 6.8 题库可展开查看 + 手动修正——也是"匹配把握不足"的人工兜底

**为什么要做**：6.1（题干去重/OCR自学）和6.4（题库同一性）本质是同一个"求吻合度"问题，都复用 `AnswerStore.canonical()`/`similarity()`（归一化后 Levenshtein 相似度）。但纯算法的"把握"天生不牢：阈值高了漏并（同题被拆成两条/两库），低了误并（两题被并成一条、答案互相覆盖横跳）。案例组合题、OCR 噪声都会制造例外。**与其追求算法 100% 置信，不如让用户能看、能改**——算法尽力且取保守（宁漏并不误并），剩下的错由用户顺手修，改完即 ground truth。这才是最务实的"把握"来源。

**要做**：`MainActivity` 题库管理界面，每个题库**点击可展开**，列出它下面所有"题干 → 已学答案"条目，用户能：
- **查看**：扫一眼有没有明显错的（答案被 OCR 读错、两题被误并成一条、同题漏并成两条）。
- **修正**：改某条的答案、删除某条；（可选）把两条手动合并、或把误并的一条拆开。
- 改动直接落盘到该题库 JSON，下次答题直接命中修正后的值。

**实现要点**：`AnswerStore` 已有 `listBanks()`、每库 JSON 里就是 `answers:{题干→字母}`，读出来展开即可；需要新增"编辑某条/删除某条/写回 JSON"的接口。题库文件结构不用变。

**配套（写进 6.1/6.4 的匹配策略）**：
- 匹配阈值取**保守**（偏向漏并），因为误并比漏并更难被用户发现、危害更大（答案互相覆盖）；漏并最多是题库多几条、靠 6.8 合并即可。
- **把"正确答案是否一致"作为文字相似度之外的独立佐证**：文字相似且答案相同→更敢判同题；文字相似但答案不同→很可能是两题或识别错，宁可不并。

## 7. 环境与工具备忘

见内存条目 [`reference-build-commands`](C:\Users\huangcan\.claude\projects\E--code-new\memory\reference-build-commands.md)：JDK/Gradle/adb的本地绝对路径、一键编译装机命令、重装后要重新打开无障碍服务的两条 `settings put` 命令、截图必须用 `adb shell screencap` 而不能用 PowerShell `>` 重定向。
